package com.vip.apitest.luxury.magento;

import static com.jayway.restassured.RestAssured.given;
import static org.hamcrest.Matchers.equalTo;

import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;

import org.junit.Before;
import org.junit.Test;

import com.jayway.restassured.RestAssured;
import com.vip.apitest.luxury.common.CommonMethod;
import com.vip.apitest.utility.MD5Encoder;

public class ShopLoginTest {
	
	 @Before
		public void setUp(){
			RestAssured.baseURI = "http://192.168.44.79";
			RestAssured.port = 80;
		}
	
	@Test
	public void testShopLoginAtion() throws NoSuchAlgorithmException{
		System.out.println("商户登录");
		String sign = MD5Encoder.encodeByMD5("shop" + "login" + "viplux#123@", MessageDigest.getInstance("MD5"));
		given().
			params("sign", sign).
			params("appid", CommonMethod.appid).
			params("username", "").
			params("password", "").
		when().
			get("/shop/login/").prettyPrint();
//		then().
//			statusCode(200).
//			body("code", equalTo(1)).
//			body("data", equalTo(true));			
	}
}
