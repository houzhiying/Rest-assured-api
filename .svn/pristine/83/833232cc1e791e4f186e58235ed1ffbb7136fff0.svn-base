package com.vip.apitest.luxury.magento;

import static com.jayway.restassured.RestAssured.given;
import static com.jayway.restassured.module.jsv.JsonSchemaValidator.matchesJsonSchema;
import static org.hamcrest.Matchers.equalTo;

import java.io.InputStream;
import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;

import org.junit.Before;
import org.junit.Test;

import com.jayway.restassured.RestAssured;
import com.vip.apitest.luxury.common.CommonMethod;
import com.vip.apitest.utility.MD5Encoder;

public class ShopOrderTest {
	
	 @Before
		public void setUp(){
			RestAssured.baseURI = "http://192.168.44.79";
			RestAssured.port = 80;
		}
	
	@Test
	public void testShopOrderAtion() throws NoSuchAlgorithmException{
		System.out.println("根据店铺shop_id列出所有已到店订单");
		InputStream jsonSchema = Thread.currentThread().getContextClassLoader().getResourceAsStream("shop_order.json");
		String sign = MD5Encoder.encodeByMD5("shop" + "order" + "viplux#123@", MessageDigest.getInstance("MD5"));
		given().
			params("sign", sign).
			params("appid", CommonMethod.appid).
			params("shop_id", "10002").
			params("type", "0").
		when().
			get("/shop/order/").
		then().
			statusCode(200).
			body("code", equalTo(1)).
			body(matchesJsonSchema(jsonSchema));		
	}
}
